syntax = "proto3";
import "google/protobuf/empty.proto";

package chat;


service Chat{
	rpc ReceiveIncomingMessages(Empty) returns (stream IncomingMessage);
	rpc SendMessage(IncomingMessage) returns (Empty);

}

message Empty{

}

message IncomingMessage{
	string from_addr=1;
	string to_addr=2;
	bytes msg=3;
}